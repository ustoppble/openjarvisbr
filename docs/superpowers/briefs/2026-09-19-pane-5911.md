# Briefing pane-5911 (gpt-6-astra max) — pegar o que o pane-5834 deixou em voo

Repo /Users/laschuk/Developer/bside/jarvis, branch main. Leia antes
docs/superpowers/briefs/2026-09-18-fechar-autonomia.md (régua e estado dos 7 itens).
Regras: card no OverClick antes de cada item, commit por pathspec com o ID, nunca
`git add -A`, nunca pedir/digitar/guardar senha, nunca imprimir chave ou endereço.
`CARGO_TARGET_DIR=/Users/laschuk/Developer/bside/jarvis/target-5834` (cache pronto).
Verifique em worktree limpo: `/tmp/jarvis-verify` já existe (`git -C /tmp/jarvis-verify
checkout --detach main` e copie os arquivos alterados para lá antes de testar).
Divisão: **pane-5882** é dono de `crates/core/src/engine.rs` (está no JRV-85). Você fica
com `crates/core/src/reflex/**`, `crates/core/src/config.rs`, `tools/**`, `crates/cli/**`.
Não editem o mesmo arquivo.

## Estado do main (HEAD 0863058 + commits do pane-5834)
- Suíte verde: 266 core + CLI, `--test-threads=1`, clippy -D warnings ok.
- Régua da última medição (docs/superpowers/briefs/2026-09-18-medicao-apos-jrv107.md):
  duplicatas 0, falhas 0, recusas 0. **Reprova só num item**: o segundo "abre a globo"
  não sai pelo reflexo.

## Trabalho em voo no working tree (NÃO commitado) — verificar, testar, commitar

1. `crates/core/src/reflex/mod.rs`: quando a decisão é `Nothing`, loga
   `reflexo: juiz respondeu mas nada passou do limiar respostas=… perguntas=…`
   (função `answers_summary`). Foi isso que mostrou a causa raiz abaixo. Commit `[JRV]`
   feat(reflex): trace das respostas do juiz.
2. `crates/core/src/reflex/decide.rs`: `learned_candidates` ranqueia por tokens em comum e,
   com casamento forte (≥2 tokens), oferece só esse nível (`MAX_LEARNED_STRONG = 5`).
   Teste novo `candidatas_aprendidas_casamento_forte_exclui_as_fracas`. Causa raiz
   (trace /tmp/jarvis-globo.out, 03:04:03): o Jev recebeu 20 opções que só compartilham
   "abre" e respondeu `learned=l20 0.40` (opção certa, probabilidade diluída, limiar 0.85).
   Commit `[JRV-1xx]` (card novo: "segundo 'abre a globo' pelo reflexo").
3. `crates/core/src/config.rs`: `ACTIONS_RULE` ("responda apenas 'Feito.'") agora também
   entra no `system_prompt` customizado do usuário (`effective_system_prompt`); o dono tem
   prompt próprio no config.toml, por isso o Jarvis ainda narrava ("Abri o site da Globo
   para você"). Teste ampliado em `prompt_padrao_manda_responder_so_feito_depois_de_agir`.
   Commit `[JRV]`.
   Rode: `cargo test -p openjarvisbr-core -- reflex:: config::` e clippy no worktree.

## Achado novo para o pane-5882 (engine.rs) — passe a ele por pane_write, com o log

/tmp/jarvis-globo.out, 03:04:04–03:04:06: depois de "abre a globo", o modelo pediu
`web.open` com args IDÊNTICOS (`https://www.globo.com`) três vezes (call_181537,
call_181543, call_181561) e as três executaram; nenhuma linha "modelo repetiu a mesma
chamada nesta fala". Ou seja, mesmo depois do JRV-107 o dedup modelo→modelo não pegou.
Hipóteses: `attributed_phrase` ficou None (o "reflexo aprendeu a ação" logo depois
sugere que não); ou `model_calls` foi esvaziado (`retain` da folga na fala nova?) ou a
janela; ou o TurnComplete entre as chamadas mexe em algo. Reproduzir no fake: UserText,
ToolCall g1, TurnComplete, ToolCall g2 (mesmos args), TurnComplete, ToolCall g3 → g2 e g3
devem vir "já executada" e spy.count()==1. Card novo (JRV-108).

## Depois
- Quando 5882 fechar JRV-85 e JRV-108: `tools/rodada_final.sh` (fecha o app, compila
  release, roda o roteiro, aplica a régua, grava a medição em docs/superpowers/briefs).
  Atenção: o mic configurado (Shure MV7+) pode não estar ligado; passe
  `--device-in "MacBook Pro Microphone"` ao runner se o CLI sair com exit 3
  ("dispositivo de entrada não encontrado") — ou faça o modo `--script` não abrir mic
  (card pequeno em crates/cli/src/script.rs + engine: `device_in` opcional no modo texto).
- Régua passou → handoff ao pane-5834 com a medição commitada. Régua não passou → o
  relatório diz o item; corrige, mede de novo. Loop até 0/0/0 com o segundo "abre a globo"
  em origem=reflexo.
